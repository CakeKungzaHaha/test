{
  "name": "us-stock-econometrics-dashboard-v8-econometrics-pro-v6-free-fixed",
  "version": "6.0.0",
  "private": true,
  "description": "US stock dashboard using free no-key SEC EDGAR financial data and Stooq/Yahoo fallback price data.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}